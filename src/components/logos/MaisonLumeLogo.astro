---
import { getLocale, localizedPath, localizeDeep, t } from "../../i18n/index";
const locale = getLocale(Astro.url);
interface Props {
  variant?: "full" | "mark";
  class?: string;
}
const { variant = "full", class: className = "" } = localizeDeep(Astro.props, locale);
---

<span class={`inline-flex items-center gap-3 ${className}`}>
  <svg viewBox="0 0 40 40" class="h-9 w-9 shrink-0" aria-hidden="true">
    <rect x="6" y="6" width="28" height="28" rx="2" fill="none" stroke="var(--primary)" stroke-width="1.6" />
    <path d="M13 27V15l7-4 7 4v12" fill="none" stroke="var(--accent)" stroke-width="1.6" stroke-linejoin="round" />
    <path d="M13 27h14" stroke="var(--primary)" stroke-width="1.6" stroke-linecap="round" />
  </svg>
  {variant === "full" && (
    <span class="leading-tight">
      <span class="block font-[var(--font-heading)] text-xl font-semibold tracking-tight text-[var(--ink)]">
        {t(locale, "Maison Lume")}
      </span>
      <span class="block -mt-1 text-[0.65rem] font-medium uppercase tracking-[0.2em] text-[var(--ink-muted)]">
        {t(locale, "Móveis &amp; decoração")}
      </span>
    </span>
  )}
</span>
